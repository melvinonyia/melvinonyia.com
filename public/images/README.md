# Error page illustrations

Files referenced by the 404 + 500 views.

- `error-404.png` — committed. CRT TV "404" test pattern.
- `error-500.svg` — not yet committed. Drop the artwork here and it
  renders automatically.

If a file is missing, the `<img>` falls back to an empty image (the
`alt=""` keeps it silent for screen readers).
